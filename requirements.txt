Django>=5.1,<5.2
gunicorn>=23.0
whitenoise>=6.7
django-cors-headers>=4.4
requests>=2.32

# Database: Postgres in production via DATABASE_URL (psycopg 3 driver);
# dj-database-url parses the URL, falling back to SQLite locally.
dj-database-url>=2.2
psycopg[binary]>=3.2

# Async server for streaming endpoints (run via gunicorn's uvicorn worker).
uvicorn>=0.30

# LLM gateway — one interface to many providers (Groq, Gemini, …) with failover.
litellm>=1.55
# litellm lazily imports its proxy/MCP modules on the tool-calling path, which need
# fastapi. Without it, any tool-using chat crashes with "No module named 'fastapi'".
fastapi>=0.110
# Loads a local .env so provider keys work in development (no-op in production,
# where the host injects env vars).
python-dotenv>=1.0

# Agent runtime — LangGraph builds/runs the agent; langchain-litellm lets it call
# models through the LiteLLM gateway above.
langgraph>=0.2
langchain-litellm>=0.1

# Async HTTP client for the GitHub tools.
httpx>=0.27

# Encrypts LLM API keys at rest (admin-managed credentials).
cryptography>=42
